# Requirements for Aillio R1 V2 GUI Application
# 
# This includes both the demo script dependencies and PyQt for the GUI

# USB communication library
pyusb>=1.3.1

# GUI framework (choose one)
# PyQt6 is recommended for newer systems
PyQt6>=6.4.0

# Alternative: PyQt5 for older systems or compatibility
# PyQt5>=5.15.0

# Optional: For Windows users
# libusb-package>=1.0.26.3; platform_system=='Windows'

# Note: If you encounter issues with PyQt6, you can install PyQt5 instead:
# pip uninstall PyQt6
# pip install PyQt5
